# Filtrar las fechas desde junio 2024
df_filtered = df_comparativo[df_comparativo['Date'] >= '2024-06-01']

# Convertir las fechas a cadenas si es necesario para usarlas como etiquetas
df_filtered['Formatted_Date'] = df_filtered['Date'].dt.strftime('%Y-%m-%d')

# Gráfico
plt.figure(figsize=(12, 6))

# Trazar las líneas usando las fechas como eje X
plt.plot(df_filtered['Formatted_Date'], df_filtered['Central'], label='Escenario Central', linewidth=2)
plt.plot(df_filtered['Formatted_Date'], df_filtered['Downside'], label='Escenario Pesimista', linestyle='--')
plt.plot(df_filtered['Formatted_Date'], df_filtered['Upside'], label='Escenario Optimista', linestyle='-.')

# Configurar el eje X para incluir todas las fechas como etiquetas
plt.xticks(rotation=45)

# Personalizar el gráfico
plt.title("Comparación de Escenarios desde Junio 2024", fontsize=16)
plt.xlabel("Fecha", fontsize=12)
plt.ylabel("Valores", fontsize=12)
plt.legend()
plt.grid(True)
plt.tight_layout()

# Mostrar el gráfico
plt.show()
